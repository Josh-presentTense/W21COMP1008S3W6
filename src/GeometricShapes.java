public interface GeometricShapes {
    //This method is abstract which means any class that
    //implements the interface, must have this method with
    //a method body to define what the method will do
    public double getArea();
}
